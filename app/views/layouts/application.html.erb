<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <nav class="navbar navbar-light bg-light">
        <%= link_to "На главную", root_path, class: 'btn-primary btn my-2 my-sm-1' %>
        <% if authorize? %>
          <%= link_to "Личный кабинет #{current_user.name}", lk_path, class: 'btn-success btn my-2 my-sm-1' %>
          <%= link_to "Выйти", logout_path, method: :delete %>
        <% else %>
          <%= link_to 'Войти', login_path, class: 'btn-dark btn my-2 my-sm-0' %>

        <%end%>
      </nav>
    <%= yield %>
  </body>
</html>
